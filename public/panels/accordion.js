window.LiveStudioPanels = window.LiveStudioPanels || {};
window.LiveStudioPanels.accordion = function (panel, helpers) {
  const api = helpers.api; const NN = ["C","C#","D","D#","E","F","F#","G","G#","A","A#","B"];
  const noteOpts = (sel) => { let o = ""; for (let m = 48; m <= 84; m++) o += `<option value="${m}"${m === sel ? " selected" : ""}>${NN[m % 12]}${Math.floor(m / 12) - 1}</option>`; return o; };
  const PARAMS = [["musette","Musette",0,40,1,12],["reediness","Reediness",0,1,0.05,0.4],["attack","Attack",0.02,1,0.02,0.15],["release","Release",0.05,1,0.05,0.3],["length","Length",0.3,6,0.1,2]]; let lastAudio = null;
  panel.innerHTML = `<div class="panel-head"><h1>🪗 Accordion</h1><p>Two detuned reed oscillators per note (musette) with clipped-sine reed timbre and a slow bellows envelope.</p></div>
    <div class="ss-toolbar"><label class="hint">Root</label><select id="ac-note">${noteOpts(60)}</select><label class="hint">Chord</label><select id="ac-chord"><option selected>single</option><option>maj</option><option>min</option><option>maj7</option></select><button class="btn" id="ac-go"><i class="ti ti-music" aria-hidden="true"></i> Synthesize &amp; import</button><button class="btn ghost" id="ac-aud"><i class="ti ti-player-play" aria-hidden="true"></i> Audition</button><span class="hint" id="ac-info"></span></div>
    <div id="ac-params" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:8px 16px;margin-top:12px"></div><div id="ac-wave" style="margin-top:12px"></div><audio id="ac-audio" style="display:none"></audio>`;
  panel.querySelector("#ac-params").innerHTML = PARAMS.map(([k, l, mn, mx, st, df]) => `<div style="display:flex;align-items:center;gap:8px"><label class="hint" style="width:80px">${l}</label><input class="ac-p" data-k="${k}" type="range" min="${mn}" max="${mx}" step="${st}" value="${df}" style="flex:1" /><span class="hint ac-pv" style="width:44px;text-align:right">${df}</span></div>`).join("");
  panel.querySelectorAll(".ac-p").forEach((s) => s.oninput = (e) => e.target.parentElement.querySelector(".ac-pv").textContent = e.target.value);
  function params() { const p = { note: +panel.querySelector("#ac-note").value, chord: panel.querySelector("#ac-chord").value }; panel.querySelectorAll(".ac-p").forEach((s) => p[s.dataset.k] = +s.value); return p; }
  function wave(pk) { if (!pk || !pk.length) return ""; const W = 640, H = 90, mid = H / 2, bw = (W - 4) / pk.length; let r = `<svg viewBox="0 0 ${W} ${H}" style="width:100%;background:#13131a;border:1px solid #2f2f36;border-radius:8px"><line x1="0" y1="${mid}" x2="${W}" y2="${mid}" stroke="#22222a"/>`; pk.forEach((p, i) => { const h = Math.max(0.6, p * (H - 10)), x = 2 + i * bw; r += `<rect x="${x.toFixed(1)}" y="${(mid - h / 2).toFixed(1)}" width="${Math.max(0.6, bw - 0.3).toFixed(1)}" height="${h.toFixed(1)}" fill="#e0985e" opacity="0.85"/>`; }); return r + `</svg>`; }
  async function synth(demo) { panel.querySelector("#ac-info").textContent = "Synthesizing…"; const r = await api.post("/api/accordion", { params: params(), import: !demo, demo: !!demo }); if (!r.success) { if (!demo) return synth(true); panel.querySelector("#ac-info").textContent = r.error || "Failed"; return; } lastAudio = r.data.audio; panel.querySelector("#ac-wave").innerHTML = wave(r.data.wave); const n = +panel.querySelector("#ac-note").value; panel.querySelector("#ac-info").textContent = `${NN[n % 12]}${Math.floor(n / 12) - 1} ${r.data.chord} · ${r.data.durSec}s${r.data.importedPath ? " · imported" : ""}`; }
  async function aud() { if (!lastAudio) await synth(true); if (!lastAudio) return; const a = panel.querySelector("#ac-audio"); a.src = lastAudio; a.currentTime = 0; a.play().catch(() => {}); }
  panel.querySelector("#ac-go").onclick = () => synth(false); panel.querySelector("#ac-aud").onclick = aud; synth(true);
};
