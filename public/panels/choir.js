window.LiveStudioPanels = window.LiveStudioPanels || {};
window.LiveStudioPanels.choir = function (panel, helpers) {
  const api = helpers.api; const NN = ["C","C#","D","D#","E","F","F#","G","G#","A","A#","B"];
  const noteOpts = (sel) => { let o = ""; for (let m = 48; m <= 84; m++) o += `<option value="${m}"${m === sel ? " selected" : ""}>${NN[m % 12]}${Math.floor(m / 12) - 1}</option>`; return o; };
  const PARAMS = [["voices","Voices",1,5,1,3],["detune","Detune",0,50,1,12],["vibrato","Vibrato",0,100,5,15],["attack","Attack",0.1,3,0.1,0.6],["release","Release",0.1,5,0.1,1.2],["length","Length",0.5,8,0.1,3]]; let lastAudio = null;
  panel.innerHTML = `<div class="panel-head"><h1>👥 Choir</h1><p>Synthesize a choir / vocal "aah" pad in-host — stacked detuned saws through vocal formants — then audition and import.</p></div>
    <div class="ss-toolbar"><label class="hint">Root</label><select id="ch-note">${noteOpts(60)}</select><label class="hint">Chord</label><select id="ch-chord"><option>single</option><option selected>maj</option><option>min</option><option>maj7</option><option>min7</option></select><label class="hint">Vowel</label><select id="ch-vowel"><option value="a" selected>aah</option><option value="o">oh</option><option value="u">oo</option><option value="aa">ah (open)</option></select>
      <button class="btn" id="ch-go"><i class="ti ti-users" aria-hidden="true"></i> Synthesize &amp; import</button><button class="btn ghost" id="ch-aud"><i class="ti ti-player-play" aria-hidden="true"></i> Audition</button><span class="hint" id="ch-info"></span></div>
    <div id="ch-params" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:8px 16px;margin-top:12px"></div><div id="ch-wave" style="margin-top:12px"></div><audio id="ch-audio" style="display:none"></audio>`;
  panel.querySelector("#ch-params").innerHTML = PARAMS.map(([k, label, min, max, step, def]) => `<div style="display:flex;align-items:center;gap:8px"><label class="hint" style="width:78px">${label}</label><input class="ch-p" data-k="${k}" type="range" min="${min}" max="${max}" step="${step}" value="${def}" style="flex:1" /><span class="hint ch-pv" style="width:44px;text-align:right">${def}</span></div>`).join("");
  panel.querySelectorAll(".ch-p").forEach((s) => s.oninput = (e) => e.target.parentElement.querySelector(".ch-pv").textContent = e.target.value);
  function params() { const p = { note: +panel.querySelector("#ch-note").value, chord: panel.querySelector("#ch-chord").value, vowel: panel.querySelector("#ch-vowel").value }; panel.querySelectorAll(".ch-p").forEach((s) => p[s.dataset.k] = +s.value); return p; }
  function wave(peaks) { if (!peaks || !peaks.length) return ""; const W = 640, H = 90, mid = H / 2, bw = (W - 4) / peaks.length; let r = `<svg viewBox="0 0 ${W} ${H}" style="width:100%;background:#13131a;border:1px solid #2f2f36;border-radius:8px"><line x1="0" y1="${mid}" x2="${W}" y2="${mid}" stroke="#22222a"/>`; peaks.forEach((p, i) => { const h = Math.max(0.6, p * (H - 10)), x = 2 + i * bw; r += `<rect x="${x.toFixed(1)}" y="${(mid - h / 2).toFixed(1)}" width="${Math.max(0.6, bw - 0.3).toFixed(1)}" height="${h.toFixed(1)}" fill="#bc8df0" opacity="0.85"/>`; }); return r + `</svg>`; }
  async function synth(demo) { panel.querySelector("#ch-info").textContent = "Synthesizing…"; const r = await api.post("/api/choir", { params: params(), import: !demo, demo: !!demo }); if (!r.success) { if (!demo) return synth(true); panel.querySelector("#ch-info").textContent = r.error || "Failed"; return; } lastAudio = r.data.audio; panel.querySelector("#ch-wave").innerHTML = wave(r.data.wave); const n = +panel.querySelector("#ch-note").value; panel.querySelector("#ch-info").textContent = `${NN[n % 12]}${Math.floor(n / 12) - 1} ${r.data.chord} · ${r.data.durSec}s${r.data.importedPath ? " · imported" : ""}`; }
  async function audition() { if (!lastAudio) await synth(true); if (!lastAudio) return; const a = panel.querySelector("#ch-audio"); a.src = lastAudio; a.currentTime = 0; a.play().catch(() => {}); }
  panel.querySelector("#ch-go").onclick = () => synth(false); panel.querySelector("#ch-aud").onclick = audition; synth(true);
};
