{
  "name": "live-studio",
  "version": "1.1.1",
  "description": "Live Studio — modular super-extension for Ableton Live: 132 modules, 382 tools, 83 quick-actions, AI copilot and a Cmd-K command palette in a single tabbed webview.",
  "keywords": [
    "ableton",
    "ableton-live",
    "extension",
    "music-production",
    "midi",
    "audio",
    "daw",
    "ai"
  ],
  "license": "GPL-3.0-or-later",
  "main": "dist/extension.js",
  "type": "module",
  "scripts": {
    "build": "tsx build.ts",
    "typecheck": "tsc --noEmit",
    "gen:demo": "python3 scripts/gen-demo-tools.py",
    "gen:catalog": "tsx scripts/gen-catalog.ts",
    "new:module": "tsx scripts/new-module.ts",
    "test": "tsx test/smoke.ts",
    "dev": "tsx build.ts && extensions-cli run",
    "package": "tsx build.ts && find . -name '.DS_Store' -not -path './node_modules/*' -delete; rm -f live-studio.ablx && find manifest.json dist -type f -not -name '.DS_Store' -not -name '*.map' -print0 | xargs -0 zip -q -X live-studio.ablx && python3 scripts/verify-ablx.py",
    "start": "extensions-cli run"
  },
  "dependencies": {
    "@ableton-extensions/sdk": "file:/Users/imac/Documents/extensions-sdk-1.0.0-beta.0/ableton-extensions-sdk-1.0.0-beta.0.tgz"
  },
  "devDependencies": {
    "@ableton-extensions/cli": "file:/Users/imac/Documents/extensions-sdk-1.0.0-beta.0/ableton-extensions-cli-1.0.0-beta.0.tgz",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.11.0"
  }
}
